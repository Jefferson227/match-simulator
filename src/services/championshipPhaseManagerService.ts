import { ChampionshipState } from '../reducers/types';

export const mountGroupsForNextPhase = (championshipState: ChampionshipState): void => {
  // Take the 1st, 4th, 5th, and 8th teams as the group A
  // Take the 2nd, 3rd, 6th, and 7th teams as the group B
  // Create the season calendar for both groups
};

export const moveToNextPhase = (): void => {};
